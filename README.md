# To-Do-list
A futuristic browser-based to-do list app built with vanilla HTML, CSS, and JavaScript. Features task priorities, due dates, subtasks, custom categories, drag &amp; drop reordering, search, filtering, and sorting. No backend, no frameworks, no installation required — all data is saved locally in the browser via localStorage.
